# Testing the plugin

## Overview

The package uses Node's built-in test runner. The tests exercise the public
PostCSS plugin across supported syntax, diagnostics, type validation, and
safety boundaries.

## Running the tests

From this package:

```sh
npm test
```

From the repository root:

```sh
npm test --workspace @microsoft/postcss-at-function
```
